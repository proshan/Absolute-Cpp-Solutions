#include <iostream>
#include <cstdlib>
using namespace std;
int main(){
    /* What is the function of the int argument to the void function exit?
    */
    //Ans: If it's exit(1), then it says that the program executed with error
    //      If it's exit(0), then it says that the program executed successfully.
}
